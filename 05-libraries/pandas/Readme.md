# Pandas
A high-performance, in-memory data manipulation library built on top of NumPy, designed for tabular and time-series data.

## Key Concepts - Prerequisite

### What is Vectorization?
- Performing operations on an entire array (or column) at once instead of looping through elements one by one.