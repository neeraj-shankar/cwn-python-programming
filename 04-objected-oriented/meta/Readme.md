# Meta Classes
- Simply put: **A metaclass is a class for a class.**

## 1. Everything is an Object
- To understand metaclasses, you have to accept a weird Python truth: Classes are objects too.
- In Python, **type** is the default metaclass. It is the "factory" that builds classes.

## 2. The **`type()`** Function's Secret Identity
